<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>DearMoryBooth</title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700&display=swap" rel="stylesheet">

<style>

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:'Inter', sans-serif;
}

/* Animations */
@keyframes fadeInUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes scaleIn{from{opacity:0;transform:scale(0.95)}to{opacity:1;transform:scale(1)}}
@keyframes pulse{0%,100%{transform:scale(1)}50%{transform:scale(1.02)}}

/* 🌫 Apple Style Background */
body{
min-height:100vh;
background: linear-gradient(135deg, #ffe6f3 0%, #ffd9f0 25%, #ffc0e4 50%, #ffb8e6 75%, #ffaae8 100%);
display:flex;
justify-content:center;
align-items:center;
position:relative;
}

body::before{
content:'';
position:fixed;
top:0;left:0;right:0;bottom:0;
background:radial-gradient(circle at 20% 50%, rgba(255,107,182,0.05) 0%, transparent 50%),
           radial-gradient(circle at 80% 80%, rgba(200,58,255,0.05) 0%, transparent 50%);
pointer-events:none;
z-index:-1;
}


.main{
width:90%;
max-width:850px;
padding:30px;
border-radius:28px;
backdrop-filter: blur(30px) saturate(180%);
background: linear-gradient(135deg, rgba(255,255,255,0.8) 0%, rgba(255,230,245,0.7) 50%, rgba(255,200,230,0.6) 100%);
box-shadow:0 30px 80px rgba(200,58,255,0.2), inset 0 2px 10px rgba(255,255,255,0.6), inset 0 -10px 30px rgba(200,58,255,0.05);
text-align:center;
border: 2px solid rgba(255,200,230,0.35);
animation:fadeInUp 0.8s ease-out;
}


h1{
font-size:2.3rem;
font-weight:700;
background: linear-gradient(90deg,#ff6bb6,#c83aff);
-webkit-background-clip:text;
color:transparent;
animation:fadeIn 1s ease-out 0.2s both;
}

h3{
background:linear-gradient(90deg, #ff6bb6 0%, #c83aff 100%);
-webkit-background-clip:text;
background-clip:text;
color:transparent;
font-weight:700;
font-size:1.4rem;
margin-bottom:12px;
position:relative;
z-index:1;
}

.subtitle{
color:#c83aff;
margin-bottom:25px;
background:linear-gradient(90deg, #ff6bb6, #c83aff, #ff6bb6);
-webkit-background-clip:text;
background-clip:text;
color:transparent;
font-weight:500;
}

/* Sections */
.section{
margin-top:25px;
padding:20px;
border-radius:20px;
background: linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(255,245,250,0.8) 50%, rgba(255,230,245,0.75) 100%);
box-shadow:0 12px 35px rgba(200,58,255,0.1), inset 0 1px 8px rgba(255,255,255,0.8), inset 0 -2px 8px rgba(200,58,255,0.05);
border: 2px solid rgba(255,200,230,0.3);
backdrop-filter: blur(12px);
animation:fadeInUp 0.8s ease-out both;
position:relative;
overflow:hidden;
}

.section::before{
content:'';
position:absolute;
top:0;right:-50%;
width:100%;
height:100%;
background:radial-gradient(circle, rgba(255,107,182,0.08) 0%, transparent 60%);
pointer-events:none;
}

.section > * {
position:relative;
z-index:1;
}


video{
width:240px;
background:white;
padding:10px;
border-radius:12px;
box-shadow:0 12px 28px rgba(200,58,255,0.15), inset 0 2px 6px rgba(255,255,255,0.4);
margin-top:12px;
border: 1px solid rgba(255,200,230,0.2);
}


button{
margin-top:15px;
padding:13px 28px;
border-radius:40px;
border:none;
font-weight:600;
cursor:pointer;
color:white;
background: linear-gradient(145deg,#ff6bb6,#c83aff);
box-shadow:
inset 0 2px 8px rgba(255,255,255,0.6),
inset 0 -2px 6px rgba(0,0,0,0.08),
0 12px 30px rgba(200,58,255,0.2);
transition:all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

button:hover{
transform:translateY(-4px) scale(1.08);
box-shadow:
inset 0 2px 8px rgba(255,255,255,0.7),
inset 0 -2px 6px rgba(0,0,0,0.1),
0 16px 40px rgba(200,58,255,0.3);
}


textarea{
width:100%;
height:130px;
margin-top:15px;
border-radius:14px;
border:2px solid rgba(255,200,230,0.2);
padding:15px;
background: linear-gradient(135deg, rgba(255,255,255,0.95), rgba(255,245,250,0.9));
outline:none;
box-shadow:inset 0 2px 6px rgba(200,58,255,0.05), inset 0 1px 3px rgba(255,255,255,0.4);
transition:all 0.3s ease;
}

textarea:focus{
border-color:rgba(200,58,255,0.3);
box-shadow:inset 0 2px 6px rgba(200,58,255,0.08), inset 0 1px 3px rgba(255,255,255,0.4), 0 0 0 3px rgba(200,58,255,0.1);
}

input{
margin-top:12px;
padding:10px;
border-radius:12px;
border:2px solid rgba(255,200,230,0.2);
background: linear-gradient(135deg, rgba(255,255,255,0.95), rgba(255,245,250,0.9));
box-shadow:inset 0 2px 6px rgba(200,58,255,0.05), inset 0 1px 3px rgba(255,255,255,0.4);
transition:all 0.3s ease;
}

input:focus{
border-color:rgba(200,58,255,0.3);
box-shadow:inset 0 2px 6px rgba(200,58,255,0.08), inset 0 1px 3px rgba(255,255,255,0.4), 0 0 0 3px rgba(200,58,255,0.1);
}


.strip{
	display:grid;
	grid-template-columns:repeat(3, 1fr);
	gap:10px;
	margin-top:15px;
	width:100%;
	max-width:100%;
	background:linear-gradient(135deg, rgba(255,200,230,0.1) 0%, rgba(200,58,255,0.05) 100%);
	padding:12px;
	border-radius:16px;
	border:2px solid rgba(255,200,230,0.15);
}

.strip figure{position:relative;margin:0;border-radius:8px;overflow:hidden;box-shadow:0 10px 28px rgba(200,58,255,0.12), inset 0 1px 4px rgba(255,255,255,0.3);animation:scaleIn 0.4s ease-out;transition:all 0.3s ease}
.strip figure:hover{transform:scale(1.08);box-shadow:0 14px 36px rgba(200,58,255,0.18), inset 0 1px 4px rgba(255,255,255,0.3)}
.strip img{
	width:100%;
	aspect-ratio:1/1;
	object-fit:cover;
	display:block;
}
.strip figcaption{
	position:absolute;
	left:0;right:0;bottom:0;
	padding:8px 10px;
	font-size:0.9rem;
	color:#fff;
	background:linear-gradient(180deg, rgba(0,0,0,0), rgba(0,0,0,0.45));
	transform:translateY(100%);
	transition:transform .22s ease;
}
.strip figure:hover figcaption{transform:translateY(0)}

@media (max-width:900px){
	.strip{grid-template-columns:repeat(3, 1fr);} 
}
@media (max-width:640px){
	.strip{grid-template-columns:repeat(2, 1fr);} 
}
@media (max-width:420px){
	.strip{grid-template-columns:repeat(1, 1fr);} 
}

/* Side-by-side photobooth layout */
.photobooth-row{display:flex;gap:24px;align-items:flex-start;flex-wrap:wrap}
.camera-col{flex:1 1 360px;min-width:260px;max-width:520px;text-align:center}
.gallery-col{flex:1 1 320px;min-width:220px}
.camera-col video{width:100%;max-width:480px;height:auto;border-radius:12px}
@media (max-width:820px){
  .photobooth-row{flex-direction:column;align-items:center}
}

/* Prominent circular capture button */
.capture{
	margin:0;
	width:76px;
	height:76px;
	padding:0;
	border-radius:50%;
	display:flex;
	align-items:center;
	justify-content:center;
	font-size:1.6rem;
	line-height:1;
	border:none;
	cursor:pointer;
	color:white;
	background: linear-gradient(135deg,#ff6bb6 0%, #c83aff 100%);
	box-shadow:inset 0 2px 8px rgba(255,255,255,0.5), inset 0 -2px 6px rgba(0,0,0,0.08), 0 16px 40px rgba(200,58,255,0.25);
	transition:all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}
.capture:hover{transform:scale(1.12);box-shadow:inset 0 2px 8px rgba(255,255,255,0.6), inset 0 -2px 6px rgba(0,0,0,0.1), 0 20px 50px rgba(200,58,255,0.35)}
.capture:active{transform:scale(0.96)}
.capture:focus{outline:3px solid rgba(255,107,182,0.18);outline-offset:4px}
.capture:hover{transform:translateY(-4px) scale(1.04)}

@media (max-width:420px){
	.capture{width:60px;height:60px;font-size:1.3rem}
}

/* Message */
#message{
margin-top:15px;
font-weight:600;
background:linear-gradient(90deg, #ff49a0 0%, #c83aff 100%);
-webkit-background-clip:text;
background-clip:text;
color:transparent;
animation:fadeIn 0.6s ease-out;
}

/* 🐾 Cute Pets GIFs */
.pets{
	display:flex;
	justify-content:center;
	gap:12px;
	margin-top:12px;
	flex-wrap:wrap;
}
.pets img{
	width:100px;
	height:100px;
	object-fit:cover;
	border-radius:12px;
	box-shadow:0 12px 28px rgba(200,58,255,0.12), inset 0 1px 4px rgba(255,255,255,0.3);
	transition:all 0.3s ease;
	animation:fadeIn 0.6s ease-out;
}
.pets img:hover{transform:scale(1.1) rotate(2deg);box-shadow:0 16px 40px rgba(200,58,255,0.2)}

.footer{
margin-top:15px;
font-size:12px;
background:linear-gradient(90deg, #ff6bb6, #c83aff, #ff6bb6);
-webkit-background-clip:text;
background-clip:text;
color:transparent;
font-weight:600;
animation:fadeIn 1.2s ease-out;
}

/* Countdown overlay + flash */
#countdown{
	position:fixed;
	left:50%;
	top:40%;
	transform:translate(-50%,-50%);
	font-size:6rem;
	color:white;
	pointer-events:none;
	text-shadow:0 6px 20px rgba(0,0,0,0.6);
	display:none;
	z-index:1001;
}
#flash{
	position:fixed;
	left:0;top:0;right:0;bottom:0;
	background:white;
	opacity:0;
	pointer-events:none;
	z-index:1000;
}

</style>
</head>

<body>

<div class="main">

<h1>📸 Snap & Send</h1>
<p class="subtitle">Capture your moment. Deliver it to your future self.</p>

<div class="section">

<h3>Photobooth</h3>

<video id="video" autoplay muted playsinline></video>
<br>
<div style="display:flex;gap:10px;align-items:center;justify-content:center;margin-top:8px;">
		<div style="display:flex;align-items:center;gap:8px;">
				<label for="timerSelect" style="font-weight:600">Timer:</label>
				<select id="timerSelect" aria-label="Capture timer">
						<option value="0">Off</option>
						<option value="3">3s</option>
						<option value="5">5s</option>
						<option value="10">10s</option>
				</select>
		</div>
</div>
<div style="display:flex;justify-content:center;align-items:center;margin-top:12px;">
		<button class="capture" onclick="triggerCapture()" aria-label="Take photo">●</button>
</div>

<div class="strip" id="strip"></div>

</div>

<div class="section">

<h3>Future Letter</h3>

<textarea id="letter" placeholder="Dear future me..."></textarea>

<input type="date" id="futureDate">

<br>
<button onclick="saveMemory()">Send to Future</button>

<p id="message"></p>

</div>

<div class="section">
	<h3>Cute Pets</h3>
	<div class="pets">
		<img src="https://media.giphy.com/media/JIX9t2j0ZTN9S/giphy.gif" alt="cute cat">
		<img src="https://media.giphy.com/media/mlvseq9yvZhba/giphy.gif" alt="cute puppy">
		<img src="https://media.giphy.com/media/3oriO0OEd9QIDdllqo/giphy.gif" alt="kitten">
		<img src="https://media.giphy.com/media/6uMqzcbWRhoT6/giphy.gif" alt="cute pup">
	</div>
</div>

<p class="footer">Time capsule memories ✦</p>

</div>

<!-- countdown + flash overlays -->
<div id="countdown" aria-hidden="true"></div>
<div id="flash" aria-hidden="true"></div>

<script>

const video = document.getElementById("video");
const strip = document.getElementById("strip");
// countdown + flash overlays (created in DOM below)
const countdownEl = document.getElementById('countdown');
const flashEl = document.getElementById('flash');

navigator.mediaDevices.getUserMedia({ video: true })
	.then(stream => {
		video.srcObject = stream;
		// some browsers require an explicit play call
		video.play().catch(() => {});
	})
	.catch(err => {
		console.error('Camera error:', err);
		document.getElementById('message').innerText = 'Camera unavailable or permission denied.';
	});

function takePhoto(){

	// ensure video has data
	if (video.readyState < 2) {
		alert('Camera not ready — try again');
		return;
	}

	// use an offscreen canvas so we don't need a DOM canvas element
	const canvas = document.createElement('canvas');
	const context = canvas.getContext('2d');

	canvas.width = 240;
	canvas.height = 180;

	context.drawImage(video, 0, 0, 240, 180);

	/* add to photostrip: wrap in figure with caption */
	let img = document.createElement('img');
	img.src = canvas.toDataURL('image/png');

	const figure = document.createElement('figure');
	const caption = document.createElement('figcaption');
	caption.innerText = new Date().toLocaleTimeString();
	figure.appendChild(img);
	figure.appendChild(caption);
	strip.appendChild(figure);

}

// Trigger capture with optional countdown
function triggerCapture(){
	const secs = parseInt(document.getElementById('timerSelect').value, 10) || 0;
	if(secs > 0){
		startCountdown(secs);
	} else {
		takePhoto();
	}
}

function startCountdown(seconds){
	if(!countdownEl) return takePhoto();
	let remaining = seconds;
	countdownEl.style.display = 'block';
	countdownEl.innerText = remaining;
	const interval = setInterval(()=>{
		remaining -= 1;
		if(remaining > 0){
			countdownEl.innerText = remaining;
		} else {
			clearInterval(interval);
			countdownEl.style.display = 'none';
			// flash effect
			if(flashEl){
				flashEl.style.transition = 'none';
				flashEl.style.opacity = '0.95';
				setTimeout(()=>{ flashEl.style.transition = 'opacity 360ms ease'; flashEl.style.opacity = '0'; }, 60);
			}
			takePhoto();
		}
	}, 1000);
}

function saveMemory(){

const letter = document.getElementById("letter").value;
const date = document.getElementById("futureDate").value;

if(letter === "" || date === ""){
alert("Complete everything first!");
return;
}

const memory = {
letter: letter,
date: date,
photos: strip.innerHTML
};

localStorage.setItem("futureMemory", JSON.stringify(memory));

document.getElementById("message").innerText =
"💿 Memory sealed for future you.";
}

</script>

</body>
</html>

